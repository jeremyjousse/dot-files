[env]
# supports arbitrary env vars so mise can be used like direnv/dotenv
#NODE_ENV = 'development'

[tools]
bun = 'latest'
deno = 'latest'
node = 'lts'
python = '3.13'
java = "temurin-jre-21.0.4+7.0.LTS"

[plugins]
